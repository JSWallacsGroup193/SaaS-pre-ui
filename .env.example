# Example environment variables
# Copy to .env and fill values

# CORS Configuration (Security: comma-separated list of allowed origins)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# Leave empty for development (defaults to localhost:5000,0.0.0.0:5000)
ALLOWED_ORIGINS=

DATABASE_URL=
JWT_SECRET=
NODE_ENV=production
OPENAI_API_KEY=
PORT=
